package com.equadis.acc.command.api.commands.account.impl;

import com.equadis.acc.command.api.commands.account.CommandHandler;

public class OpenAccountCommand implements CommandHandler {
    @Override
    public void handle(OpenAccountCommand command) {

    }
}
